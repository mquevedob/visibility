# v0.3 Roadmap

## Roadmap intent

v0.3 builds on the completed v0.2 deterministic one-product analyzer.

v0.2 made the diagnostic core clearer by validating finding taxonomy cleanup, query-level visibility health, explicit URL evidence, focused HTML fixtures, and a minimal local scenario CLI.

v0.3 should make the analyzer more useful for realistic merchant-facing demos and future integrations without changing the core product boundary.

The theme for v0.3 is:

> Report usability and scenario realism.

The goal is not to add live data collection yet. The goal is to make local deterministic scenarios richer, reports easier to consume, and scenario authoring less error-prone.

## Progress tracking rules

This roadmap is the source of truth for v0.3 implementation progress.

Every phase must keep this progress block directly under the phase title:

```md
**Status:** Pending  
**Progress:** 0%  
**Implemented in:** —  
**Validated by:** —
```

Allowed statuses:

- `Pending` — not started.
- `In Progress` — implementation has started but is incomplete or not fully validated.
- `Implemented` — code has been created, but acceptance criteria are not fully validated yet.
- `Validated` — implementation is complete and tests/acceptance criteria pass.
- `Blocked` — implementation cannot proceed until a dependency, design decision, or bug is resolved.
- `Skipped` — intentionally removed from v0.3 scope.

Progress rules:

- Use `0%` for `Pending`.
- Use an approximate percentage for `In Progress`.
- Use `80%` to `99%` for `Implemented` when tests or acceptance validation are still pending.
- Use `100%` only for `Validated`.
- Update `Implemented in` with commit SHA, branch name, or PR reference when available.
- Update `Validated by` with the exact test command, manual check, or acceptance evidence used.
- Do not mark a phase as `Validated` unless its acceptance criteria pass.
- Do not change the phase scope silently while marking progress. If scope changes, update the phase text explicitly.

Codex and future implementation chats should update these fields whenever a phase is started, implemented, blocked, skipped, or validated.

## Progress summary

- Total phases: 5
- Validated: 3
- Implemented: 0
- In progress: 0
- Pending: 2
- Blocked: 0
- Skipped: 0

## v0.3 product boundary

The v0.3 core remains centered on this API shape:

```php
VisibilityAnalyzer::analyze(
    ProductSubject $product,
    array $queries
): VisibilityReport;
```

Where:

- `$product` is one product.
- `$queries` contains one or more query contexts for that product.
- Search-result evidence is still supplied from outside the core through a provider or scenario fixture.
- Page evidence is still supplied through a fetcher abstraction or scenario fixture.
- The analyzer returns one `VisibilityReport` for that product.

There is no `BatchVisibilityAnalyzer` in v0.3.

The minimal CLI remains scenario-driven:

```bash
bin/visibility analyze examples/scenarios/visible-clean.json
```

v0.3 may improve CLI output modes, scenario validation, and scenario coverage, but it must not turn the CLI into a crawler, scraper, SaaS worker, or batch monitoring tool.

## v0.3 non-goals

Do not implement these in v0.3:

- live Google scraping;
- live Bing scraping;
- marketplace scraping;
- Google Shopping scraping;
- AI answer-engine checks;
- embeddings;
- vector databases;
- semantic product-query matching;
- automatic expected-query generation;
- browser rendering;
- full-site crawling;
- Laravel integration;
- dashboard UI;
- SaaS account/project model;
- scheduled monitoring;
- competitor reports;
- batch analysis for many products;
- revenue forecasting or attribution modeling;
- external HTTP fetching as default CLI behavior.

## v0.3 design principles

- Keep the analyzer deterministic.
- Keep one-product analysis as the central unit.
- Prefer richer local scenarios over live external collection.
- Prefer small Codex-sized phases over broad feature batches.
- Preserve v0.2 status semantics: `visible`, `not_visible`, `uncertain`.
- Preserve v0.2 visibility health semantics: `healthy`, `at_risk`, `blocked`, `unknown`.
- Preserve v0.2 URL evidence roles: `matchedUrl`, `requestedUrl`, `finalUrl`, `expectedUrl`, `canonicalUrl`.
- Preserve existing JSON report compatibility unless a change is explicitly documented and tested.
- Make reports easier for humans to consume without removing structured evidence needed by machines.

## Phase 1 — Page transport and indexability scenarios

**Status:** Validated  
**Progress:** 100%  
**Implemented in:** PR #30  
**Validated by:** composer test

### Goal

Add deterministic local scenarios for page transport and indexability failures that are already part of the MVP diagnostic promise.

### Problem

v0.2 validates clean, HTML noindex, canonical mismatch, and missing Product schema cases. The MVP success criteria also include HTTP failure, redirect/final URL problems, and `X-Robots-Tag` noindex, but these are not yet represented as focused CLI scenarios.

This phase should expand scenario coverage without mixing schema/content work into the same implementation prompt.

### Deliverables

Add deterministic local scenarios under `examples/scenarios/` and supporting fixtures under `examples/fixtures/` as needed for:

- product page HTTP error;
- product page redirect to a different product, category, or other non-equivalent URL;
- `X-Robots-Tag` noindex.

### Acceptance criteria

- Each new scenario is executable with `bin/visibility analyze <scenario-path>`.
- Each scenario isolates one primary diagnostic condition where practical.
- Each scenario has a test proving the expected finding code appears.
- Existing v0.2 scenarios continue to work.
- No scenario requires network calls, browser rendering, scraping, or external services.
- This phase does not add schema, offer, or content-alignment scenarios.
- This phase does not add compact output or merchant-facing summaries.
- `vendor/bin/phpunit` passes after implementation.

### Design notes

Prefer PageSnapshot-level fixture evidence for HTTP and redirect cases instead of creating misleading HTML-only fixtures.

For `X-Robots-Tag`, prefer explicit header evidence in the page fixture/scenario payload.

Do not implement batch scenario execution in this phase.

### Codex prompt seed

```text
Implement Phase 1 of docs/roadmap/v0.3.md: Page transport and indexability scenarios.

Goal:
- Add deterministic local CLI scenarios for HTTP error, redirect/final URL mismatch, and X-Robots-Tag noindex cases.

Requirements:
- Add scenario JSON files under examples/scenarios/ for:
  1. product page HTTP error;
  2. product page redirect/final URL to a different product, category, or other non-equivalent URL;
  3. X-Robots-Tag noindex.
- Add minimal supporting fixtures under examples/fixtures/ only when needed.
- Prefer PageSnapshot-level scenario evidence for HTTP and redirect cases.
- Prefer explicit response header evidence for X-Robots-Tag noindex.
- Each scenario should isolate one primary diagnostic condition where practical.
- Add tests proving each scenario emits its expected finding code.
- Keep existing v0.2 scenarios working.
- Do not add schema/offer/content scenarios in this phase.
- Do not add compact output, merchant-facing summaries, live scraping, browser rendering, external HTTP calls, batch analysis, Laravel, SaaS, or dashboards.

Validation policy:
- Do not run composer, phpunit, PHP, npm, artisan, linters, formatters, installers, migrations, builds, git status, git diff, or other local validation commands.
- Do not claim validation commands were run.
- Owner will run vendor/bin/phpunit after implementation.
```

## Phase 2 — Structured data and content scenarios

**Status:** Validated  
**Progress:** 100%  
**Implemented in:** PR #31  
**Validated by:** composer test

### Goal

Add deterministic local scenarios for structured data and content-alignment failures that affect product discoverability.

### Problem

v0.2 includes a missing Product schema scenario, but the MVP success criteria also mention Product schema lacking Offer, price, currency, or availability, and product page content that is weakly aligned with expected product terms.

Those cases are distinct from page transport/indexability failures and should be implemented in a separate Codex-sized phase.

### Deliverables

Add deterministic local scenarios under `examples/scenarios/` and supporting fixtures under `examples/fixtures/` as needed for:

- canonical points to homepage;
- Product schema present but Offer missing;
- Product schema Offer present but missing price;
- Product schema Offer present but missing currency;
- Product schema Offer present but missing availability;
- weak title/H1/body alignment with expected terms.

### Acceptance criteria

- Each new scenario is executable with `bin/visibility analyze <scenario-path>`.
- Each scenario isolates one primary diagnostic condition where practical.
- Each scenario has a test proving the expected finding code appears.
- Existing v0.2 and Phase 1 v0.3 scenarios continue to work.
- No scenario requires network calls, browser rendering, scraping, or external services.
- This phase does not add compact output or merchant-facing summaries.
- `vendor/bin/phpunit` passes after implementation.

### Design notes

The exact finding codes should follow the existing detector taxonomy. If a required diagnostic does not yet exist, add the smallest deterministic detector change needed and cover it with tests.

Do not add AI/semantic content matching. Content alignment should remain deterministic using existing expected terms and parsed page evidence.

Do not implement batch scenario execution in this phase.

### Codex prompt seed

```text
Implement Phase 2 of docs/roadmap/v0.3.md: Structured data and content scenarios.

Goal:
- Add deterministic local CLI scenarios for structured data and content-alignment diagnostic cases.

Requirements:
- Add scenario JSON files under examples/scenarios/ for:
  1. canonical points to homepage;
  2. Product schema present but Offer missing;
  3. Product schema Offer present but missing price;
  4. Product schema Offer present but missing currency;
  5. Product schema Offer present but missing availability;
  6. weak title/H1/body alignment with expected terms.
- Add minimal supporting HTML fixtures under examples/fixtures/.
- Each scenario should isolate one primary diagnostic condition where practical.
- Add tests proving each scenario emits its expected finding code.
- If a required deterministic diagnostic does not exist yet, add the smallest detector change needed and test it.
- Keep existing v0.2 scenarios and Phase 1 v0.3 scenarios working.
- Do not add AI/semantic matching, compact output, merchant-facing summaries, live scraping, browser rendering, external HTTP calls, batch analysis, Laravel, SaaS, or dashboards.

Validation policy:
- Do not run composer, phpunit, PHP, npm, artisan, linters, formatters, installers, migrations, builds, git status, git diff, or other local validation commands.
- Do not claim validation commands were run.
- Owner will run vendor/bin/phpunit after implementation.
```

## Phase 3 — Compact report output mode

**Status:** Validated  
**Progress:** 100%  
**Implemented in:** PR #32  
**Validated by:** composer test

### Goal

Add a compact report output mode so CLI demos can show the key diagnostic result without printing full page HTML, full parsed page evidence, and every nested evidence reference by default.

### Problem

The existing JSON report is useful as a complete machine-readable artifact, but it is too large for quick terminal inspection and merchant-facing demos. A clean scenario can produce a large JSON payload because it includes `pageSnapshot.body`, parsed page structures, and nested evidence.

The full report should remain available, but developers need a smaller view for quick diagnosis.

### Deliverables

- A compact output mode for the CLI, for example:

```bash
bin/visibility analyze examples/scenarios/visible-clean.json --compact
```

- A compact JSON shape that includes the essential information only:
  - product identity;
  - overall status;
  - overall priority;
  - query statuses;
  - visibility health;
  - top probable causes;
  - top recommended actions;
  - URL evidence summary;
  - warnings.
- Full output remains the default unless a different choice is explicitly justified.
- README documentation for compact output.

### Acceptance criteria

- Full JSON output remains available and backward compatible.
- Compact output omits full `pageSnapshot.body`.
- Compact output omits large parsed page internals unless needed for a top-level diagnostic.
- Compact output remains valid JSON.
- Compact output is deterministic and tested.
- CLI errors still go to stderr with non-zero exit codes.
- `vendor/bin/phpunit` passes after implementation.

### Design notes

Prefer implementing compact output as a serializer or report projector rather than mutating the core `VisibilityReport` object.

Do not remove evidence from the full report.

Do not introduce a non-JSON text format in this phase.

### Codex prompt seed

```text
Implement Phase 3 of docs/roadmap/v0.3.md: Compact report output mode.

Goal:
- Add a compact JSON output mode for CLI scenario analysis.

Requirements:
- Add CLI support for a compact flag, for example --compact.
- Full JSON output must remain the default and remain backward compatible.
- Compact output must be valid JSON and include product identity, summary status, query statuses, visibilityHealth, top probable causes, top recommended actions, URL evidence summary, and warnings.
- Compact output must omit full pageSnapshot.body and large parsed page internals.
- Implement compact output as a serializer/projector rather than changing the core report object destructively.
- Add tests for full output still containing expected report sections and compact output omitting large evidence while keeping key diagnostics.
- Update README usage docs.
- Do not add non-JSON output, external services, scraping, browser rendering, Laravel, SaaS, dashboards, or batch analysis.

Validation policy:
- Do not run composer, phpunit, PHP, npm, artisan, linters, formatters, installers, migrations, builds, git status, git diff, or other local validation commands.
- Do not claim validation commands were run.
- Owner will run vendor/bin/phpunit after implementation.
```

## Phase 4 — Merchant-facing summary layer

**Status:** Pending  
**Progress:** 0%  
**Implemented in:** —  
**Validated by:** —

### Goal

Improve the report summary so it is more useful to a merchant or ecommerce operator, while preserving the existing technical finding evidence.

### Problem

The current summary is technically useful, but it can still read like a developer diagnostic. Informational findings such as `product.visible_in_results` may dominate a healthy report, and the summary does not yet clearly separate:

- current visibility result;
- technical health;
- likely commercial implication;
- recommended next action.

The project objective requires translating invisibility into actionable commercial insight, not merely listing technical issues.

### Deliverables

Add a merchant-facing summary section or projector that clearly presents:

- `visibilityOutcome` — what happened in supplied search evidence;
- `technicalHealth` — whether the product page is healthy, at risk, blocked, or unknown;
- `commercialRisk` — none, low, medium, high, or critical based on product/query priority and finding severity;
- `plainLanguageSummary` — one short human-readable diagnosis;
- `primaryIssue` — the top actionable issue when one exists;
- `nextBestAction` — the recommended fix to do first.

### Acceptance criteria

- Healthy visible scenarios do not read as if there is a problem.
- Not-visible scenarios clearly state the product was absent from supplied results.
- Blocked/at-risk visible scenarios clearly state that visibility exists but technical discoverability risk remains.
- The summary separates technical diagnosis from commercial implication.
- Existing structured findings and evidence remain available.
- Existing full JSON report compatibility is preserved unless a change is explicitly documented and tested.
- `vendor/bin/phpunit` passes after implementation.

### Design notes

Prefer adding a new summary projection over changing low-level finding generation.

Do not use LLM-generated text.

Keep messages deterministic and template-based.

### Codex prompt seed

```text
Implement Phase 4 of docs/roadmap/v0.3.md: Merchant-facing summary layer.

Goal:
- Add a clearer merchant-facing summary projection while preserving existing technical findings and evidence.

Requirements:
- Add a deterministic summary section or projector with visibilityOutcome, technicalHealth, commercialRisk, plainLanguageSummary, primaryIssue, and nextBestAction.
- Healthy visible scenarios must not read like a problem.
- Not-visible scenarios must clearly say the expected product was absent from supplied results.
- Visible but blocked/at-risk scenarios must clearly say the product appeared but technical discoverability risk remains.
- Separate technical diagnosis from commercial implication.
- Preserve existing structured findings, evidence, status values, visibilityHealth values, and URL evidence roles.
- Add tests for visible healthy, not visible, visible noindex/blocked, visible canonical at-risk, and missing schema scenarios.
- Do not use LLM-generated text or external APIs.
- Do not add scraping, browser rendering, Laravel, SaaS, dashboards, or batch analysis.

Validation policy:
- Do not run composer, phpunit, PHP, npm, artisan, linters, formatters, installers, migrations, builds, git status, git diff, or other local validation commands.
- Do not claim validation commands were run.
- Owner will run vendor/bin/phpunit after implementation.
```

## Phase 5 — Scenario documentation and demo workflow cleanup

**Status:** Pending  
**Progress:** 0%  
**Implemented in:** —  
**Validated by:** —

### Goal

Make the repository easier to understand, demo, and extend after v0.3 scenario and reporting improvements.

### Problem

The repository now has README usage examples, PHP scripts, fixture scenarios, CLI support, and multiple roadmaps. As the scenario set and reporting modes grow, documentation needs to clearly explain which path a developer should use and which artifacts are historical.

Scenario JSON validation is important, but it is better handled in a future v0.4 because v0.3 already has enough implementation scope with scenario expansion, compact output, and merchant-facing summaries.

### Deliverables

- README update reflecting v0.3 capabilities.
- Clear distinction between:
  - PHP example scripts;
  - scenario JSON CLI workflow;
  - full JSON report;
  - compact JSON report;
  - merchant-facing summary.
- Scenario index table documenting each scenario, primary condition, expected status, expected visibility health, and expected top finding.
- Roadmap links updated to include v0.2 and v0.3.
- Optional `examples/scenarios/README.md` if the README becomes too large.

### Acceptance criteria

- A new developer can run the recommended demo command without reading old roadmap files.
- Each included scenario is documented with its purpose.
- README does not imply that the package performs live search, scraping, crawling, AI checks, SaaS monitoring, or batch analysis.
- Historical PHP scripts remain documented as compatibility examples if they are still present.
- `vendor/bin/phpunit` passes after implementation.

### Design notes

Do not over-document internal implementation details.

The documentation should reinforce positioning: deterministic product visibility diagnostics core.

Do not add new scenario validation behavior in this phase.

### Codex prompt seed

```text
Implement Phase 5 of docs/roadmap/v0.3.md: Scenario documentation and demo workflow cleanup.

Goal:
- Update documentation so v0.3 capabilities are easy to understand and demo.

Requirements:
- Update README to explain the recommended scenario CLI workflow, full JSON output, compact JSON output if implemented, and merchant-facing summary if implemented.
- Add a scenario index table documenting each scenario, primary condition, expected status, expected visibilityHealth, and expected top finding.
- Update roadmap links to include v0.2 and v0.3.
- Keep PHP example scripts documented as compatibility examples if they still exist.
- Make sure documentation does not imply live search, scraping, crawling, AI checks, SaaS monitoring, dashboards, or batch analysis.
- Add examples/scenarios/README.md only if it keeps the main README cleaner.
- Do not change production behavior unless needed for documentation consistency.
- Do not add scenario validation behavior in this phase.

Validation policy:
- Do not run composer, phpunit, PHP, npm, artisan, linters, formatters, installers, migrations, builds, git status, git diff, or other local validation commands.
- Do not claim validation commands were run.
- Owner will run vendor/bin/phpunit after implementation.
```
